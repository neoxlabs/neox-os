<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  硬化运行时下的例外 —— 每一条都是**必须**, 不是保险起见:

    allow-jit / allow-unsigned-executable-memory / disable-library-validation:
      Electron 自带的 V8 要 JIT, 硬化运行时默认不给 —— 少一条应用起不来.

    inherit(在 Inherit 那份里): 子进程继承同一套沙箱.

  **没有 com.apple.security.app-sandbox**: 这个应用要 spawn 一台 OS,
  而那台 OS 要在用户亲手选的目录里写文件. App Sandbox 会把这件事变成
  不可能 —— 那是上架 Mac App Store 才需要的东西, Developer ID 分发不需要.
-->
<plist version="1.0">
<dict>
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
  <key>com.apple.security.cs.allow-dyld-environment-variables</key>
  <true/>
</dict>
</plist>
